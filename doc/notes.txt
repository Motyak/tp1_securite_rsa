Faudrait que je fasse le 5.
	Faire un texte alphabétique en premier (26 lettres).
		=> faire des blocs de 2.
	Pour pouvoir chiffrer du texte encodé en ASCII, on fait
		des blocs de 3 (car code ASCII ne dépasse jamais 3 digits)
____________________________

un programme pour générer une clé publique et une clé privé
	(dans 2 fichiers respectifs)

un programme pour encrypter un message avec une clé publique

un programme pour decrypter un message avec une clé privé

pouvoir utiliser mon programme avec netcat et des redirections

Gérer les exceptions si nombres trop grands pour type c++


gen_keys					#les fichiers keys s'enregistreront dans le répertoire d'où a été lancé le prog
encrypt *msg* *@clePub*		#pour passer le contenu d'un fichier en param utiliser : "$(< pub.key)"
decrypt *msg* *@clePriv*	#pour passer le contenu d'un fichier en param utiliser : "$(< priv.key)"

____________________________

Faire le chiffrement/dechiffrement de messages ASCII.
Faire un makefile ou script equiv pour build les bin.




Expliquer chaque fonction, la procédure pour générer les clés, chiffrer, dechiffrer.

Moi, plutot que de generer un e qui se doit d'être premier avec phi(n), 
en vérifiant avec un calcul pgcd (algorithme d'Euclide étendu), 
et de calculer d à partir de e, en utilisant l'inversion modulaire,
je calcule directement une paire e, d.

0)On a comme message clair un texte : "Hello".
1)On remplace chaque caractère par son code ASCII, et on les append dans une chaine initiallement vide.
	(De 0 à 127)
2)On créé des blocs de taille 4 en partant de la fin de la chaine, on convertit en int.
3)On chiffre chaque bloc avec la clé publique.
4)On retourne une chaine avec tous les blocs chiffrés à la suite, séparés d'un espace.

0)On a comme message cryptée une suite d'entiers dans une chaine.
1)on dechiffre chaque entier, on garde une chaine en résultat 
	(remplir avec des zeros si bloc plus petit que 3)
2)on créer des blocs de 4 en partant de la fin de la chaine (on prend pas le surplus de zero)
	et on convertit en int.
3)On remplace ou cast chaque int en char, on retourne un string



TODO:
1)Pouvoir chiffrer/dechiffrer un texte alphabétique (voir ASCII)
2)Trouver meilleur moyen de generer e et d ?
3)Pouvoir expliquer les algos utilisés, méthodes, procédures..
4)Ecrire les protocoles pour les exos 2 et 3